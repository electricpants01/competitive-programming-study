# Guide — Tickets

## G-1 — Document section activation map
**Status:** Done (captured in spec)  
Ensure every `data-section` is documented and linked from sidebar/nav.

## G-2 — Extract section router helpers
**Status:** Optional  
If `guide-script.js` grows, extract `setActiveSection` / sidebar rendering for unit tests.

## G-3 — Deep-link to section via hash/query
**Status:** Done  
`?section=search|videos|…` or `#search` / `#videos` opens the section on load.
