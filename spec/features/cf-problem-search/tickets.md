# CF Problem Search — Tickets

## CF-1 — Harden offline / CORS error UX
**Status:** Done  
`errorMsg` + Retry button that forces a refetch.

## CF-2 — Unit-test `applyFilterSort`
Extract pure function + fixtures for AND/OR and sorts.

## CF-3 — Export / import favorites JSON
Backlog: optional backup of localStorage maps.
