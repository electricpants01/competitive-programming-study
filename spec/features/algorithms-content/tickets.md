# Algorithms Content — Tickets

## AC-1 — Fix `bitmask-dp` duplicate `examples` key
Merge TSP + SOS DP examples into one array in EN and ES.

## AC-2 — Add second examples for listed topics
segment-tree lazy prop; modular extended Euclid + linear inverses; linear sieve + φ; Catalan + derangements.

## AC-3 — Quiz coverage audit
Ensure every topic has 5–8 conceptual questions (not pure recall).
