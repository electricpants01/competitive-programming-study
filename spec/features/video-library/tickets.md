# Video Library — Tickets

## VL-1 — Empty segments UX
**Status:** Done  
When a keyword matches the title but `segments` is empty, show `t.videos.noTranscript`.

## VL-2 — Debounced search input
**Status:** Done (160 ms debounce already in `initVideoSearch`)

## VL-3 — Refresh EN/ES data
Coordinate with Content Curator + scraper monthly.
